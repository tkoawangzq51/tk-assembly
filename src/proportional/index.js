import TKProportional from './src/proportional'

/* istanbul ignore next */
TKProportional.install = function (Vue) {
  Vue.component(TKProportional.name, TKProportional)
}

export default TKProportional
